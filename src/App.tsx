import React, { FC } from "react";

import { Layout } from "./pages/layout/Layout"

const App: FC = () => {
  return (
    <div className="container pt-3">
      <Layout />
    </div>
  );
};

export default App;
